/**
 * ARCHIVO: app.routes.ts - Definición de rutas de la aplicación
 *
 * Las rutas le dicen a Angular qué componente mostrar cuando el usuario
 * navega a una URL diferente.
 *
 * Analogía: Es como un mapa de carreteras. Cada URL es una dirección
 * y cada componente es el destino que se muestra cuando llegas a esa dirección.
 */

// Routes: Tipo de Angular que define la estructura de las rutas.
import { Routes } from '@angular/router';

// Array de rutas vacío. En una app real, aquí definirías las rutas necesarias.
export const routes: Routes = [];

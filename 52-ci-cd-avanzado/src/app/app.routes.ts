// ============================================================
// app.routes.ts — Definición de rutas de la aplicación
// ============================================================
// Las rutas le dicen a Angular qué componente mostrar según la URL.
// Es como un mapa: si el usuario va a "/dashboard", Angular muestra
// el componente Dashboard; si va a "/login", muestra Login.

// Routes es un tipo de TypeScript que define la estructura de las rutas.
import { Routes } from '@angular/router';

// Aquí irían las rutas. En este ejemplo simple no hay rutas definidas.
// Ejemplo de ruta: { path: 'dashboard', component: DashboardComponent }
export const routes: Routes = [];


/**
 * Configuración de la aplicación de Accesibilidad.
 *
 * No necesita proveedores especiales porque usa el CDK de Angular
 * (Component Dev Kit) que ya viene incluido en las dependencias del proyecto.
 */
import { ApplicationConfig } from '@angular/core';

export const appConfig: ApplicationConfig = { providers: [] };
